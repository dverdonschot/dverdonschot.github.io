{
  "name": "personal-site",
  "version": "1.0.0",
  "type": "module",
  "description": "Personal website with blog",
  "scripts": {
    "build": "node build.js",
    "serve": "npx http-server dist -p 8000 -o",
    "dev": "npm run build && npm run serve",
    "nostr:keygen": "node scripts/generate-nostr-key.js",
    "nostr:identity": "node scripts/show-nostr-identity.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "gray-matter": "^4.0.3",
    "marked": "^11.1.1",
    "nostr-tools": "^2.19.4"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
